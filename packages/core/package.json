{
  "name": "@eternity/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Domain model, event protocol, ledger math, allocation policy and market simulation for Eternity Station. Pure TypeScript, no IO.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json --outDir dist"
  },
  "dependencies": {
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.0.0"
  }
}
