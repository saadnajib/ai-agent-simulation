{
  "name": "@eternity/adapters",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Platform adapters (Printify, Etsy, Gumroad, itch.io, WordPress, manual queues) plus the simulated marketplace adapter.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@eternity/core": "workspace:*",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.0"
  }
}
